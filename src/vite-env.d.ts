/// <reference types="vite/client" />

// Injected at build time by vite.config.ts ("define") — the app version
// derived from git commit count. See CLAUDE.md → "Versioning".
declare const __APP_VERSION__: string;
